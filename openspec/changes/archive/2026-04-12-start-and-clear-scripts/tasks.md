# Tasks: Start and Clear Data Scripts

- [ ] Create `start.bat` at repo root
- [ ] Create `clear_data.bat` at repo root
- [ ] Verify `start.bat` starts the stack successfully
- [ ] Verify `clear_data.bat` wipes volumes and logs
