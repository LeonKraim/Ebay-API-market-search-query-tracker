# Tasks

- [ ] Add `_apply_env_overrides()` method to `Settings` in `backend/app/config.py`
- [ ] Call `_apply_env_overrides()` in `get_settings()` after `_apply_toml()`
- [ ] Rebuild and verify backend connects to Postgres in Docker
